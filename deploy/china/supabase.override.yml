# For the official Supabase Compose snapshot using the api-gw service (Envoy).
# Compose >= 2.24.4 supports !override / !reset. Applied by prepare-supabase.py.
services:
  api-gw:
    ports: !override ["127.0.0.1:8000:8000"]
  supavisor:
    ports: !reset []
