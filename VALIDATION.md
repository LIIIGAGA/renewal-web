# 交付验证

验证日期：2026-09-17。实际安装：Next.js 16.3.5；完整版本记录见 `package-lock.json`。

设备记住令牌更新：默认关闭，可勾选后在连接成功时将令牌与仓库配置明文保存在浏览器 localStorage；页面启动自动连接，清除 / 取消勾选保留当前连接，断开同步同时清除保存。TypeScript 检查、子路径静态构建及 12 项电脑 / 手机浏览器流程通过，包括未勾选不保存、勾选后刷新自动连接、清除、网络失败保留数据与令牌、401 拒绝后不替换保存令牌、输入新令牌成功后更新及再次自动连接。320–1440px 静态页面及设置页无横向溢出。测试使用虚构令牌与模拟 API，没有访问 renewal-data 或使用真实令牌。

分类更新：AI / 视频 / 音乐 / 其他字段、筛选、电脑表格列和手机标签已实现。TypeScript 检查及子路径静态构建通过；13 项单元测试通过，覆盖旧 JSON / CSV / GitHub 文档默认「其他」、四类导入导出及同步保留分类。8 项电脑 / 手机浏览器流程通过，覆盖分类筛选、新建预选、编辑后持久化和模拟 GitHub 跨设备同步；320、375、390、768、1440px 静态页面检查无横向溢出。Supabase 分类迁移已提供，未在真实云端数据库执行。

订阅台账 / GitHub 同步更新：默认隐藏邮件功能并停止台账模式的日期自动推进；TypeScript 检查、常规生产构建及 `/renewal-web/` 子路径静态导出构建通过。4 项 GitHub API 模拟测试通过，覆盖中文、ID 保留、私有仓库检查、未授权 404、版本冲突和非法数据。已有 4 项电脑 / 手机操作流程通过，新增 2 项双浏览器上下文同步 / 并发修改 / 令牌不持久化测试通过。

从导出的实际静态文件检查 320、375、390、768、1440px 宽度：总览、表单及同步设置无横向溢出；子路径资源无 HTTP 错误，图标、manifest 和 Service Worker 可用。截图为 `ledger-mobile.png`、`ledger-mobile-form.png` 和 `ledger-desktop.png`。GitHub 同步验证使用模拟 API，没有读取真实私有仓库、使用真实令牌或发布 Pages；大陆网络仍需你实际验收。静态导出时 Next.js 提示服务器 headers 不生效，已在使用说明标明由托管平台负责。

大陆部署更新：前端和提醒函数 TypeScript 检查、standalone 生产构建通过；数据库权限 / 租约回归、6 项邮件处理用例和 Node 提醒进程测试通过。新增测试核对阿里云请求签名、中文 / 特殊字符和主题长度、业务拒绝，以及 Node 进程只访问容器内 API、成功轮次刷新健康时间。测试使用模拟服务，不代表真实邮件送达。

本机未安装 Docker，未验证真实 Linux Compose 栈、HTTPS 证书、国内网络、Auth SMTP 或备份恢复；这些按 `DEPLOY-CHINA.md` 的上线验收完成后才能确认。配置和源码已交付，没有购买服务器、办理备案或发布云服务。

视觉更新后重新通过生产构建和 4 项桌面 / 手机浏览器测试，并检查了实际截图。新版覆盖总览、详情、表单、设置、登录、离线提示以及 PWA 图标；字体随项目提供。

光标动效更新后生产构建通过；在生产预览中检查了鼠标位置跟随、悬停、点击、原生弹窗层级、输入框、键盘操作隐藏、减少动态效果开关、移出窗口和手机触控，全部通过。交互回归的 4 个用例均报告成功，本轮测试运行器在收尾阶段未退出，终止收尾后另用生产预览完成上述验证。

| 检查 | 结果 |
| --- | --- |
| 前端 TypeScript 检查 | 通过 |
| Edge Function TypeScript 检查 | 通过 |
| Next.js 生产构建 | 通过 |
| 日期、导入导出、支出及安全校验 | 7 项通过 |
| PostgreSQL 集成测试 | 1 项通过，内含白名单/RLS隔离、月底推进、租约、去重、重试快照、停用及重新启用断言 |
| 提醒函数处理测试 | 6 项通过，覆盖未授权、成功、停用、失败、未确认邮箱及阿里云签名 / 拒绝 |
| Node 私有提醒进程 | 1 项通过，验证打包、内部 API、健康记录和失败退出 |
| 桌面与手机浏览器流程 | 4 项通过，覆盖增删改、持久化、搜索空结果、CSV下载、导入确认、停用、取消链接、PWA manifest与Service Worker |
| npm 依赖安全扫描 | 安装时 0 个已知漏洞 |

后端测试使用 PGlite 的实际 PostgreSQL 引擎，执行交付迁移中的表、权限、触发器和函数；托管环境自带的 pgcrypto / Realtime publication 不在本地复现。邮件函数测试运行交付处理代码，用模拟数据库与邮件接口验证分支，未发送真实邮件。

尚需云端配置及验证：Supabase 实际登录与跨设备同步、Realtime托管连接、Vault/pg_cron/pg_net任务调用、Resend真实投递与域名验证、Vercel正式HTTPS地址。交付中没有真实密钥，也没有声称这些云端服务已经启用。

本机因 C 盘无剩余空间，依赖和构建改在任务的 D 盘可写目录运行；输出目录和下载包只保留源码、示例与说明，不包含依赖缓存或秘密。
